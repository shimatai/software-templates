package ${{values.java_package_name}}.services.impl;

import ${{values.java_package_name}}.domain.File;
import ${{values.java_package_name}}.services.CopyProcessorService;

public class CopyProcessorServiceImpl implements CopyProcessorService {

    @Override
    public boolean copyFile(File inputFile) {

        return false;
    }
}
